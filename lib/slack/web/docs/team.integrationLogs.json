{
  "args": {
    "app_id": {
      "desc": "Filter logs to this Slack app. Defaults to all logs.",
      "required": false,
      "type": "string"
    },
    "change_type": {
      "desc": "Filter logs with this change type. Possible values are `added`, `removed`, `enabled`, `disabled`, and `updated`. Defaults to all logs. _Acceptable values:_ `added` `removed` `enabled` `disabled` `updated`",
      "example": "added",
      "required": false,
      "type": "string"
    },
    "count": {
      "default": "100",
      "desc": "",
      "required": false,
      "type": "string"
    },
    "page": {
      "default": "1",
      "desc": "",
      "required": false,
      "type": "string"
    },
    "service_id": {
      "desc": "Filter logs to this service. Defaults to all logs.",
      "required": false,
      "type": "string"
    },
    "team_id": {
      "desc": "encoded team id to get logs from, required if org token is used",
      "required": false,
      "type": "string"
    },
    "user": {
      "desc": "Filter logs generated by this user’s actions. Defaults to all logs.",
      "required": false,
      "type": "string"
    }
  },
  "desc": "Gets the integration logs for the current team.",
  "errors": { "missing_argument": "A required argument is missing." },
  "rate_limit": {
    "label": "Tier 2: 20+ per minute",
    "url": "https://docs.slack.dev/apis/web-api/rate-limits"
  },
  "scopes": {
    "user": [
      {
        "name": "admin",
        "url": "https://docs.slack.dev/reference/scopes/admin"
      }
    ]
  }
}

